# ═══════════════════════════════════════════════════════
# CompanyAI — Ortam Değişkenleri Şablonu
# ═══════════════════════════════════════════════════════
# Bu dosyayı .env olarak kopyalayıp değerlerinizi girin:
#   cp .env.example .env
# ═══════════════════════════════════════════════════════

# ── Veritabanı ──
DATABASE_URL=postgresql+asyncpg://companyai:your-strong-password@localhost:5432/companyai

# ── JWT Authentication ──
# Üretmek için: python -c "import secrets; print(secrets.token_urlsafe(64))"
SECRET_KEY=CHANGE-ME-TO-A-RANDOM-64-CHAR-STRING
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# ── Admin ──
ADMIN_DEFAULT_PASSWORD=change-this-strong-password

# ── LLM (Ollama) ──
OLLAMA_BASE_URL=http://localhost:11434
LLM_MODEL=qwen2.5:72b

# ── Redis ──
REDIS_URL=redis://localhost:6379

# ── Server ──
HOST=0.0.0.0
PORT=8000
DEBUG=false

# ── Güvenlik ──
PASSWORD_MIN_LENGTH=8
RATE_LIMIT_PER_MINUTE=30

# ── Web Arama API'leri (opsiyonel) ──
SERPAPI_KEY=
GOOGLE_API_KEY=
GOOGLE_CSE_ID=

# ── CORS ──
CORS_ORIGINS=["http://localhost:3000","http://192.168.0.12"]

# ── ChromaDB ──
CHROMA_PERSIST_DIR=/opt/companyai/data/chromadb
